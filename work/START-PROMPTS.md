# Start prompts — one screen at a time

Copy a block into Claude Code to start a screen. Each prompt forces reading the handoff and the
skills before coding, and closing the context loop after.

## Home — `tab` (P0)

```
Work on "Home" for MusicApp (tab). BEFORE coding, read: work/handoffs/home.md, the artifact work/mockups/home.html, the existing implementation in src/screens/HomeScreen.tsx, and the build-a-screen skill (+ musicapp-conventions). Follow the layered architecture (context -> src/lib + src/db -> native modules) (container App.tsx) and REUSE our shared pieces. strings via t() (fr+en), styles via useThemedStyles, Android + JDK 21. Finish with: npx tsc --noEmit && npx eslint .. Then refresh PROJECT-STATE.md and set this unit's status in work/INDEX.md.
```

## Library — `tab` (P0)

```
Work on "Library" for MusicApp (tab). BEFORE coding, read: work/handoffs/library.md, the artifact work/mockups/library.html, the existing implementation in src/screens/LibraryScreen.tsx, and the build-a-screen skill (+ musicapp-conventions). Follow the layered architecture (context -> src/lib + src/db -> native modules) (container App.tsx) and REUSE our shared pieces. strings via t() (fr+en), styles via useThemedStyles, Android + JDK 21. Finish with: npx tsc --noEmit && npx eslint .. Then refresh PROJECT-STATE.md and set this unit's status in work/INDEX.md.
```

## YouTube (native tab) — `tab` (P0)

```
Work on "YouTube (native tab)" for MusicApp (tab). BEFORE coding, read: work/handoffs/youtube.md, the artifact work/mockups/youtube.html, the existing implementation in src/screens/YoutubeScreen.tsx, and the build-a-screen skill (+ musicapp-conventions). Follow the layered architecture (context -> src/lib + src/db -> native modules) (container App.tsx) and REUSE our shared pieces. strings via t() (fr+en), styles via useThemedStyles, Android + JDK 21. Finish with: npx tsc --noEmit && npx eslint .. Then refresh PROJECT-STATE.md and set this unit's status in work/INDEX.md.
```

## YouTube video page — `modal from youtube` (P0)

```
Work on "YouTube video page" for MusicApp (modal from youtube). BEFORE coding, read: work/handoffs/youtube-video.md, the artifact work/mockups/youtube-video.html, the existing implementation in src/screens/YoutubeVideoScreen.tsx, and the build-a-screen skill (+ musicapp-conventions). Follow the layered architecture (context -> src/lib + src/db -> native modules) (container App.tsx) and REUSE our shared pieces. strings via t() (fr+en), styles via useThemedStyles, Android + JDK 21. Finish with: npx tsc --noEmit && npx eslint .. Then refresh PROJECT-STATE.md and set this unit's status in work/INDEX.md.
```

## Now playing — `modal` (P0)

```
Work on "Now playing" for MusicApp (modal). BEFORE coding, read: work/handoffs/now-playing.md, the artifact work/mockups/now-playing.html, the existing implementation in src/screens/NowPlayingScreen.tsx, and the build-a-screen skill (+ musicapp-conventions). Follow the layered architecture (context -> src/lib + src/db -> native modules) (container App.tsx) and REUSE our shared pieces. strings via t() (fr+en), styles via useThemedStyles, Android + JDK 21. Finish with: npx tsc --noEmit && npx eslint .. Then refresh PROJECT-STATE.md and set this unit's status in work/INDEX.md.
```

## Queue — `modal` (P1)

```
Work on "Queue" for MusicApp (modal). BEFORE coding, read: work/handoffs/queue.md, the artifact work/mockups/queue.html, the existing implementation in src/screens/QueueScreen.tsx, and the build-a-screen skill (+ musicapp-conventions). Follow the layered architecture (context -> src/lib + src/db -> native modules) (container App.tsx) and REUSE our shared pieces. strings via t() (fr+en), styles via useThemedStyles, Android + JDK 21. Finish with: npx tsc --noEmit && npx eslint .. Then refresh PROJECT-STATE.md and set this unit's status in work/INDEX.md.
```

## Search — `modal from header` (P1)

```
Work on "Search" for MusicApp (modal from header). BEFORE coding, read: work/handoffs/search.md, the artifact work/mockups/search.html, the existing implementation in src/screens/SearchScreen.tsx, and the build-a-screen skill (+ musicapp-conventions). Follow the layered architecture (context -> src/lib + src/db -> native modules) (container App.tsx) and REUSE our shared pieces. strings via t() (fr+en), styles via useThemedStyles, Android + JDK 21. Finish with: npx tsc --noEmit && npx eslint .. Then refresh PROJECT-STATE.md and set this unit's status in work/INDEX.md.
```

## Recents — `modal from drawer` (P2)

```
Work on "Recents" for MusicApp (modal from drawer). BEFORE coding, read: work/handoffs/recents.md, the artifact work/mockups/recents.html, the existing implementation in src/screens/RecentsScreen.tsx, and the build-a-screen skill (+ musicapp-conventions). Follow the layered architecture (context -> src/lib + src/db -> native modules) (container App.tsx) and REUSE our shared pieces. strings via t() (fr+en), styles via useThemedStyles, Android + JDK 21. Finish with: npx tsc --noEmit && npx eslint .. Then refresh PROJECT-STATE.md and set this unit's status in work/INDEX.md.
```

## Settings — `modal from drawer` (P1)

```
Work on "Settings" for MusicApp (modal from drawer). BEFORE coding, read: work/handoffs/settings.md, the artifact work/mockups/settings.html, the existing implementation in src/screens/SettingsScreen.tsx, and the build-a-screen skill (+ musicapp-conventions). Follow the layered architecture (context -> src/lib + src/db -> native modules) (container App.tsx) and REUSE our shared pieces. strings via t() (fr+en), styles via useThemedStyles, Android + JDK 21. Finish with: npx tsc --noEmit && npx eslint .. Then refresh PROJECT-STATE.md and set this unit's status in work/INDEX.md.
```

## Collection detail — `modal` (P1)

```
Work on "Collection detail" for MusicApp (modal). BEFORE coding, read: work/handoffs/collection-detail.md, the artifact work/mockups/collection-detail.html, the existing implementation in src/screens/CollectionDetailScreen.tsx, and the build-a-screen skill (+ musicapp-conventions). Follow the layered architecture (context -> src/lib + src/db -> native modules) (container App.tsx) and REUSE our shared pieces. strings via t() (fr+en), styles via useThemedStyles, Android + JDK 21. Finish with: npx tsc --noEmit && npx eslint .. Then refresh PROJECT-STATE.md and set this unit's status in work/INDEX.md.
```

## YouTube web fallback — `modal from youtube` (P2)

```
Work on "YouTube web fallback" for MusicApp (modal from youtube). BEFORE coding, read: work/handoffs/youtube-web.md, the artifact work/mockups/youtube-web.html, the existing implementation in src/screens/YoutubeWebScreen.tsx, and the build-a-screen skill (+ musicapp-conventions). Follow the layered architecture (context -> src/lib + src/db -> native modules) (container App.tsx) and REUSE our shared pieces. strings via t() (fr+en), styles via useThemedStyles, Android + JDK 21. Finish with: npx tsc --noEmit && npx eslint .. Then refresh PROJECT-STATE.md and set this unit's status in work/INDEX.md.
```

## Listening stats — `modal from drawer` (P1)

```
Build "Listening stats" for MusicApp (modal from drawer). BEFORE coding, read: work/handoffs/listening-stats.md, the existing implementation in src/screens/ListeningStatsScreen.tsx, and the build-a-screen skill (+ musicapp-conventions). Follow the layered architecture (context -> src/lib + src/db -> native modules) (container App.tsx) and REUSE our shared pieces. strings via t() (fr+en), styles via useThemedStyles, Android + JDK 21. Finish with: npx tsc --noEmit && npx eslint .. Then refresh PROJECT-STATE.md and set this unit's status in work/INDEX.md.
```

## Account connect — `modal from drawer` (P2)

```
Build "Account connect" for MusicApp (modal from drawer). BEFORE coding, read: work/handoffs/account-connect.md, the existing implementation in src/screens/AccountScreen.tsx, and the build-a-screen skill (+ musicapp-conventions). Follow the layered architecture (context -> src/lib + src/db -> native modules) (container App.tsx) and REUSE our shared pieces. strings via t() (fr+en), styles via useThemedStyles, Android + JDK 21. Finish with: npx tsc --noEmit && npx eslint .. Then refresh PROJECT-STATE.md and set this unit's status in work/INDEX.md.
```

## Identify — `modal from home header` (P1)

```
Work on "Identify" for MusicApp (modal from home header). BEFORE coding, read: work/handoffs/identify.md, the artifact work/mockups/identify.html, the existing implementation in src/screens/IdentifyScreen.tsx, and the build-a-screen skill (+ musicapp-conventions). Follow the layered architecture (context -> src/lib + src/db -> native modules) (container App.tsx) and REUSE our shared pieces. strings via t() (fr+en), styles via useThemedStyles, Android + JDK 21. Finish with: npx tsc --noEmit && npx eslint .. Then refresh PROJECT-STATE.md and set this unit's status in work/INDEX.md.
```

## Player styles — `modal from drawer` (P2)

```
Build "Player styles" for MusicApp (modal from drawer). BEFORE coding, read: work/handoffs/player-styles.md, the existing implementation in src/screens/PlayerStylesScreen.tsx, and the build-a-screen skill (+ musicapp-conventions). Follow the layered architecture (context -> src/lib + src/db -> native modules) (container App.tsx) and REUSE our shared pieces. strings via t() (fr+en), styles via useThemedStyles, Android + JDK 21. Finish with: npx tsc --noEmit && npx eslint .. Then refresh PROJECT-STATE.md and set this unit's status in work/INDEX.md.
```
